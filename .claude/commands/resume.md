---
description: Reboot a fresh session from handoff.md
---

Resume work on BOSS Blueprint:

1. Read `CLAUDE.md`, then `handoff.md`, then the active feature file named in the handoff's
   **🔗 Pointer** section.
2. Summarize your understanding back to the user in 3 bullets: what the project is, where it
   actually stands right now, and what's next.
3. Confirm the **➡️ Next Up** items from `handoff.md` with the user before doing any work.

Always apply: the verification protocol in `CLAUDE.md` (no `verified done` without a
Verification Log entry) and the truth hierarchy (code beats docs — fix stale docs on sight).
