# Failed Approaches — BOSS Blueprint

Append-only graveyard of dead ends. Never prune. `handoff.md` links here instead of carrying
the stories itself.

Entry format:

```markdown
## <date> — <What we tried>
**Why it failed:** <root cause> · **Do instead:** <the working direction, if known>
```

_No entries yet._
